import { Component, inject, signal } from '@angular/core';import { FormsModule } from '@angular/forms';import { Router } from '@angular/router';import { GameDataService } from '../../core/game-data.service';
@Component({standalone:true,imports:[FormsModule],templateUrl:'./create.page.html',styleUrl:'./create.page.scss'}) export class CreatePage{private data=inject(GameDataService);private router=inject(Router);sport=signal<'football'|'basketball'>('football');team='Kadıköy United';date='2026-09-20';time='18:30';venue='Caddebostan Spor Parkı';format='7v7';create(){this.data.addMatch({id:Date.now(),home:this.team,away:'Rakip aranıyor',date:'20 EYL',time:this.time,venue:this.venue,format:this.format,sport:this.sport(),status:'open'});this.router.navigateByUrl('/matches')}}
