import { Component, computed, inject, signal } from '@angular/core';import { FormsModule } from '@angular/forms';import { RouterLink } from '@angular/router';import { GameDataService } from '../../core/game-data.service';
@Component({standalone:true,imports:[FormsModule,RouterLink],templateUrl:'./coaches.page.html',styleUrl:'./coaches.page.scss'}) export class CoachesPage{data=inject(GameDataService);district=signal('Tümü');gym=signal('Tümü');results=computed(()=>this.data.coaches().filter(c=>(this.district()==='Tümü'||c.district===this.district())&&(this.gym()==='Tümü'||c.gym===this.gym())))}
